# Dockerfile.sync
FROM python:3.11-slim

WORKDIR /app

COPY backend/data/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/data/ ./

CMD ["python", "sync_to_google.py"]
